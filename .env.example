# Example environment variables
# MongoDB connection string (set in your host's environment)
MONGODB_URI="mongodb+srv://<user>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority"
DB_NAME="deejpotter"

# Environment indicator (development, staging, production)
NEXT_PUBLIC_ENV="development"

# Backend API URL for contact form and other API calls
NEXT_PUBLIC_BACKEND_URL="http://localhost:3001"

# Optional (if using an auth provider)
# If you use Netlify Identity externally, document how to obtain/manage tokens.
# If you move to another provider (Auth0/Clerk/Supabase), list required keys here.
