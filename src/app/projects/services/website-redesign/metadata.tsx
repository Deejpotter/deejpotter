import { generatePageMetadata } from "@/app/metadata";

export const metadata = generatePageMetadata(
  "Website Redesign Service",
  "Website redesign service by Deej Potter for sites that need clearer messaging, stronger structure, better responsiveness, and improved conversion flow.",
  "/projects/services/website-redesign"
);
