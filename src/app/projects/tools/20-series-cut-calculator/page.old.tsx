"use client";

// Legacy placeholder left intentionally empty to keep history without Bootstrap usage.
// The active cut calculator lives in page.tsx.

export default function CutCalculatorLegacy(): null {
  return null;
}
