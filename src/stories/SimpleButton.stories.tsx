import React from 'react';

const meta = {
  title: 'Debug/SimpleButton',
};

export default meta;

export const Basic = () => (
  <div style={{padding:20}}>
    <button style={{padding:'8px 16px', borderRadius:8, background:'#1E9952', color:'#fff', border:'none'}}>Simple Button</button>
  </div>
);
