export const ImageContext = {
  // Minimal stub used during tests — Storybook's next-image plugin imports ImageContext
  // Provide a minimal API that components may use
  disableStaticImages: false,
  getLoader: () => undefined
};
